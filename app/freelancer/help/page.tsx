import { HelpView } from "@/components/support/help-view";
import type { Metadata } from "next";

export const metadata: Metadata = { title: "Help & support" };

export default function FreelancerHelpPage() {
  return <HelpView />;
}
